<!-- 
	start 文章导航 
	注意，这里的 前一页pre，后一页next，需要根据条件渲染。
	所有的链接，也是需要条件渲染的。
	这里显示/隐藏不需要控制。只需要控制一个href就行。
-->
<nav class="navigation pagination py-6 px-2" id="list-page-container" data-max-page="1518"
	data-url="https://www.fanqiejsq.com/category/%E5%BD%B1%E9%9F%B3%E5%8A%A0%E9%80%9F%E5%99%A8/">

	<div class="nav-links flex justify-center items-center gap-2 sm:gap-4 flex-wrap">
		<span class="another-element"></span>

		<!-- 前一页的链接，需要做判断，如果前一页是第一页，则直接就是 pageData.url -->
		<a hidden [hidden]="pageData.page  == 1 || pageData.total < 5"
			class="prev page-numbers flex items-center gap-1 p-2 h-10  text-gray-800 bg-gray-200 rounded-md hover:bg-blue-600 hover:text-white transition-all"
			[href]="(pageData.page - 1 == 1) ?  pageData.url : pageData.url + 'page/' + (pageData.page - 1)">
			<span class="ast-left-arrow text-lg sm:text-xl ">←</span> 前一页
		</a>
		<!-- 第一页 -->
		<a class="page-numbers px-3 py-2  text-gray-800 bg-gray-200 rounded-md hover:bg-blue-600 hover:text-white transition-all"
			[href]="pageData.url">
			1
		</a>

		<!-- page 大于2的时候，这里需要显示。 [hidden]="pageData.page  >= 2" -->
		<span [hidden]="pageData.page  < 4" class="page-numbers dots  text-gray-500">…</span>

		<!-- page -1，但是如果page - 1 === 1,这里需要隐藏。 -->
		<a [hidden]="pageData.page - 1 <= 1"
			class="page-numbers px-3 py-2  text-gray-800 bg-gray-200 rounded-md hover:bg-blue-600 hover:text-white transition-all"
			[href]="(pageData.page - 1 == 1) ? pageData.url : pageData.url + 'page/' + (pageData.page - 1)"
			[text]="pageData.page - 1">
		</a>

		<!-- page 当前页码如果 === 1，也需要隐藏。 -->
		<a [hidden]="pageData.page == 1 || pageData.page == pageData.total"
			class="page-numbers px-3 py-2  text-gray-800 bg-gray-200 rounded-md hover:bg-blue-600 hover:text-white transition-all"
			[href]="pageData.url + 'page/' + pageData.page " [text]="pageData.page">

		</a>
		<!-- page + 1 , 如果 page + 1 === total 这里需要隐藏。 -->
		<a [hidden]="pageData.page + 1 >= pageData.total"
			class="after page-numbers px-3 py-2  text-gray-800 bg-gray-200 rounded-md hover:bg-blue-600 hover:text-white transition-all"
			[href]="pageData.url + 'page/' + (pageData.page + 1)" [text]="pageData.page + 1">

		</a>

		<!-- total - page > 1 , 显示，否则隐藏。 -->
		<span [hidden]="pageData.page + 1 >= pageData.total - 1 || pageData.total < 5 "
			class="page-numbers dots   text-gray-500">…</span>

		<a class="page-numbers px-3 py-2  text-gray-800 bg-gray-200 rounded-md hover:bg-blue-600 hover:text-white transition-all"
			[href]="pageData.url + 'page/' + pageData.total" [text]="pageData.total">
		</a>
		<a [hidden]="pageData.page == pageData.total || pageData.total < 5"
			class="next page-numbers flex items-center gap-1 p-2  h-10 text-gray-800 bg-gray-200 rounded-md hover:bg-blue-600 hover:text-white transition-all"
			[href]="pageData.url + 'page/' + (pageData.page + 1)">
			后一页 <span class="ast-right-arrow text-lg sm:text-xl">→</span>
		</a>
	</div>
</nav>
<!-- end 文章导航 -->